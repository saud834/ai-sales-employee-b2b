# PostgreSQL connection string (Neon/Supabase/RDS/local docker-compose all work)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ava_zero_to_billion"

# Claude API key - used server-side only (lib/ai/provider.ts), never exposed to the client
ANTHROPIC_API_KEY=""
