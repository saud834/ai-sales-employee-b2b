# Google (Veo video + Cloud TTS voice) - the path this session can reach.
# Create at https://aistudio.google.com/apikey (enable billing for Veo access).
GOOGLE_API_KEY=""

# Runway (target-stack video provider) - correct implementation included,
# but api.runwayml.com is blocked from this session's network policy.
# Only useful if run from a machine with normal internet access.
# Create at https://dev.runwayml.com
RUNWAYML_API_SECRET=""

# ElevenLabs (target-stack voice provider) - same situation as Runway:
# implemented, but api.elevenlabs.io is blocked from this session.
# Create at https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=""
